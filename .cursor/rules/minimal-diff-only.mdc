---
description: 仅按用户 prompt 要求改代码，禁止顺带改动其它内容
alwaysApply: true
---

# 最小改动 / 仅生成要求范围内的代码

在根据用户 prompt 生成或修改代码时：

1. **只改用户明确要求的部分**：实现 prompt 中描述的功能、文件、函数或片段；不要扩展范围。
2. **禁止顺带改动**：不重排无关文件、不做「顺手」格式化整文件、不删改与用户请求无关的注释或代码、不升级依赖、不加未要求的配置。
3. **禁止主动加戏**：不新增用户未要的文档（README、CHANGELOG 等）、不新增未要的测试或示例，除非 prompt 明确要求。
4. **多文件时**：仅 touch 完成任务所必需的文件；若用户只点名一个文件，默认只改该文件（除非逻辑上无法实现）。
5. **风格一致**：在**必须修改**的代码块内，保持与周边现有代码一致；不要为了「统一风格」去改未要求修改的相邻行。

若用户请求隐含需要 touching 额外文件（例如新增 import 的目标文件不存在），仅在**最小必要**范围内修改。
